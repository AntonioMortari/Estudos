//Calcular o desconto em uma compra com base em um cupom:
let valorCompra = 100
let cupom = 'DESC10'
let desconto = cupom == 'DESC10' ? valorCompra*0.10 : 0
let = valorFinal = valorCompra - desconto
console.log(valorFinal)